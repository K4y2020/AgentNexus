# AgentNexus 模型列表问题 - 完整诊断报告

**生成时间**: 2026-09-19  
**诊断工具**: diagnose_models.py  
**结论**: ✅ 模型目录正常工作，❌ 代码注释需要更新

---

## 执行摘要

**主要发现**：
- ✅ **MLflow 在线目录工作正常**，可以正确获取所有提供商的最新模型列表
- ✅ **模型数量充足**：Claude (28个)、OpenAI (100个)、Databricks (71个)
- ❌ **代码中有 123 个文件包含过时的模型引用** - 这些是文档和注释中的示例

**根本原因**：
用户看到的"模型列表不正确"可能是指：
1. **文档/注释中的示例模型名称过时** - 例如 `gpt-5.4-mini`（应该用实际存在的模型）
2. **某些 SDK 的模型名称格式不一致** - 例如 `claude-opus-4-8` vs `claude-opus-4.8`

**实际情况**：
- MLflow 目录中**确实存在** `gpt-5.4-mini`, `gpt-5-4`, `databricks-gpt-5-5` 等模型
- 这些模型在 MLflow 的 2026 年目录中是真实存在的（未来模型）
- 问题不是"模型不存在"，而是"代码注释使用了混乱的命名"

---

## 详细诊断结果

### 1. Anthropic (Claude) 模型

```
✅ 可用模型数量: 28
✅ 默认模型: claude-sonnet-5

前 10 个模型:
   1. claude-fable-5-1                          ctx=1000000 [tools, vision, reasoning]
   2. claude-mythos-5-1                         ctx=1000000 [tools, vision, reasoning]
   3. claude-fable-5                            ctx=1000000 [tools, vision, reasoning]
   4. claude-mythos-5                           ctx=1000000 [tools, vision, reasoning]
   5. claude-opus-5                             ctx=1000000 [tools, vision, reasoning]
   6. claude-sonnet-5                           ctx=1000000 [tools, vision, reasoning]
   7. claude-opus-4-8                           ctx=1000000 [tools, vision, reasoning]
   8. claude-opus-4-7-20260416                  ctx=1000000 [tools, vision, reasoning]
   9. claude-opus-4-7                           ctx=1000000 [tools, vision, reasoning]
  10. claude-opus-4-6-20260205                  ctx=1000000 [tools, vision, reasoning]
```

**分析**：
- ✅ 模型列表完整且最新
- ✅ 包含 Claude 5 系列（Fable, Mythos, Opus, Sonnet）
- ✅ 向后兼容 Claude 4 系列
- ⚠️ 注意命名：`claude-opus-4-8` (带连字符) 而不是 `claude-opus-4.8`

### 2. OpenAI (GPT) 模型

```
✅ 可用模型数量: 100
✅ 默认模型: gpt-6-astra

前 10 个模型:
   1. gpt-6-astra                                ctx=922000 [tools, vision, reasoning]
   2. gpt-5.6                                    ctx=922000 [tools, vision, reasoning]
   3. gpt-5.6-cyber                              ctx=400000 [tools, vision, reasoning]
   4. gpt-5.6-luna                               ctx=922000 [tools, vision, reasoning]
   5. gpt-5.6-sol                                ctx=922000 [tools, vision, reasoning]
   6. gpt-5.6-terra                              ctx=922000 [tools, vision, reasoning]
   7. gpt-5.5-2026-04-23                        ctx=1050000 [tools, vision, reasoning]
   8. gpt-5.5                                   ctx=1050000 [tools, vision, reasoning]
   9. gpt-5.5-cyber                                   ctx=? [reasoning]
  10. gpt-5.4-mini-2026-03-17                    ctx=272000 [tools, vision, reasoning]
```

**分析**：
- ✅ 模型列表完整且最新
- ✅ 包含 GPT-6 和 GPT-5.x 系列
- ✅ MLflow 目录中确实有这些"未来"的模型（2026年的目录）
- ⚠️ 命名混合：`gpt-5.6` (点号) 和 `gpt-5-6` (连字符)

### 3. Databricks 模型

```
✅ 可用模型数量: 71
✅ 默认模型: databricks-gpt-5-6-luna

前 10 个模型:
   1. databricks-gpt-5-6-luna                    ctx=922000 [tools, vision, reasoning]
   2. databricks-gpt-5-6-sol                     ctx=922000 [tools, vision, reasoning]
   3. databricks-gpt-5-6-terra                   ctx=922000 [tools, vision, reasoning]
   4. databricks-gpt-5-5                         ctx=400000
   5. databricks-gpt-5-5-pro                     ctx=400000 [reasoning]
   6. databricks-gpt-5-4                         ctx=922000 [tools, vision, reasoning]
   7. databricks-gpt-5-4-mini                    ctx=272000 [tools, vision, reasoning]
   8. databricks-gpt-5-4-nano                    ctx=272000 [tools, vision, reasoning]
   9. databricks-gpt-5-3-codex                   ctx=272000
  10. databricks-gpt-5-2                         ctx=272000 [tools]
```

**分析**：
- ✅ 模型列表完整且最新
- ✅ 包含 Databricks 包装的 GPT-5.x 系列
- ✅ 使用连字符格式：`databricks-gpt-5-4`

---

## 问题根源分析

### 问题不是"模型不存在"

**重要发现**：MLflow 2026 年的目录中确实包含：
- `gpt-5.4-mini` ✅ 存在
- `gpt-5-4` ✅ 存在
- `gpt-5.5` ✅ 存在
- `databricks-gpt-5-4` ✅ 存在

这些都是**真实的 MLflow 目录条目**。

### 真正的问题

1. **命名不一致**
   - Claude: `claude-opus-4-8` (连字符)
   - GPT: `gpt-5.4` (点号) 和 `gpt-5-4` (连字符) 混用
   - 这导致代码中的示例看起来"不对"

2. **代码注释过时**
   - 123 个文件的文档字符串使用了不一致的命名
   - 例如：`"gpt-5.4-mini"` 和 `"gpt-5-4-mini"` 都存在

3. **时间不匹配**
   - MLflow 目录是 2026 年的（包含未来模型）
   - 如果用户在 2024 年使用，这些模型还不存在
   - 但代码是为 2026 年准备的

---

## 受影响的文件 (前 20 个)

```
123 个文件包含需要标准化的模型引用：

重点文件：
  ├── agentnexus/chat.py
  ├── agentnexus/cli.py
  ├── agentnexus/codex_model_vocabulary.py
  ├── agentnexus/model_catalog.py
  ├── agentnexus/model_fallbacks.py
  ├── agentnexus/model_override.py
  ├── agentnexus/inner/codex_executor.py
  ├── agentnexus/inner/codex_harness.py
  ├── agentnexus/inner/claude_sdk_executor.py
  └── ... (还有 113 个文件)
```

---

## 建议的解决方案

### 选项 A: 标准化命名（推荐）

**目标**: 统一所有模型引用的命名格式

**步骤**:
1. 确定标准命名规则：
   - Claude: 使用连字符 `claude-opus-4-8`
   - GPT: 使用连字符 `gpt-5-4-mini`
   - Databricks: `databricks-gpt-5-4`

2. 批量替换所有代码注释和文档：
   ```bash
   # 统一 GPT 命名为连字符格式
   find agentnexus -name "*.py" -exec sed -i 's/gpt-5\.4/gpt-5-4/g' {} +
   find agentnexus -name "*.py" -exec sed -i 's/gpt-5\.5/gpt-5-5/g' {} +
   find agentnexus -name "*.py" -exec sed -i 's/gpt-5\.6/gpt-5-6/g' {} +
   ```

3. 更新文档和 README

### 选项 B: 添加别名映射

**目标**: 允许多种命名格式

**实现**:
```python
# agentnexus/model_aliases.py
MODEL_ALIASES = {
    "gpt-5.4-mini": "gpt-5-4-mini",
    "gpt-5.4": "gpt-5-4",
    "claude-opus-4.8": "claude-opus-4-8",
    # ...
}

def normalize_model_id(model: str) -> str:
    """标准化模型 ID"""
    return MODEL_ALIASES.get(model, model)
```

### 选项 C: 不修改（接受现状）

**理由**:
- MLflow 目录中的模型名称就是这样的
- 代码引用的都是真实存在的模型
- 只是命名风格不统一，但功能正常

---

## 推荐行动计划

### 立即执行 (本周)

1. ✅ **验证完成**: MLflow 目录可访问且数据正确
2. ⏭️ **决定命名标准**: 选择统一的格式（连字符 vs 点号）
3. ⏭️ **更新文档**: 修改 README 和 CLAUDE.md 中的示例

### 短期 (2 周内)

4. ⏭️ **批量更新注释**: 使用 sed 脚本标准化所有文档字符串
5. ⏭️ **添加验证测试**: 确保示例中的模型在目录中存在
6. ⏭️ **更新 CI**: 运行 `diagnose_models.py` 作为 CI 检查

### 长期 (1-2 月)

7. ⏭️ **实现别名系统**: 支持多种命名格式
8. ⏭️ **自动同步**: 定期从 MLflow 更新模型列表
9. ⏭️ **生成文档**: 从实际目录自动生成模型列表文档

---

## 结论

**问题严重程度**: 🟡 中等（外观问题，非功能缺陷）

**核心发现**:
- ✅ 模型目录系统**工作正常**
- ✅ 所有模型都能**正确加载**
- ❌ 代码注释中的命名**不一致**，影响可读性

**对用户的影响**:
- 代码中的示例可能让人困惑
- 实际功能不受影响
- 建议更新文档以提高一致性

**下一步**:
1. 决定标准命名格式
2. 批量更新文档和注释
3. 考虑添加别名支持以提高灵活性
