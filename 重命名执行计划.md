# AgentNexus 完整重命名执行计划

## 目标
将所有 `omnigent` 引用改为 `agentnexus`，建立独立品牌标识

## 执行顺序（从低风险到高风险）

### Phase 1: 准备工作
- [x] 备份当前状态（git commit）
- [ ] 创建重命名分支
- [ ] 记录当前测试通过状态

### Phase 2: 包名和模块重命名

#### 2.1 重命名 Python 包目录
```bash
git mv omnigent agentnexus
```

#### 2.2 批量替换导入语句
```bash
# Python 文件中的导入
find . -name "*.py" -type f -exec sed -i 's/from omnigent/from agentnexus/g' {} +
find . -name "*.py" -type f -exec sed -i 's/import omnigent/import agentnexus/g' {} +
find . -name "*.py" -type f -exec sed -i 's/"omnigent/"agentnexus/g' {} +
find . -name "*.py" -type f -exec sed -i "s/'omnigent/'agentnexus/g" {} +
```

### Phase 3: 配置文件

#### 3.1 pyproject.toml
```toml
[project]
name = "agentnexus"  # 原 omnigent
version = "0.12.0.dev0"

[project.scripts]
agentnexus = "agentnexus.cli:main"
nexus = "agentnexus.cli:main"
# 移除 omnigent/omni 别名

[tool.uv.sources]
agentnexus-client = { path = "sdks/python-client", editable = true }
agentnexus-ui-sdk = { path = "sdks/ui", editable = true }
agentnexus-slack = { path = "integrations/slack", editable = true }

[tool.setuptools.packages.find]
include = ["agentnexus*"]  # 原 omnigent*
exclude = ["agentnexus.server.static", "agentnexus.server.static.*"]
```

#### 3.2 setup.py
```python
# 包名引用
name="agentnexus"
packages=find_packages(include=["agentnexus*"])
```

#### 3.3 package.json (根目录和 web/)
```json
{
  "name": "agentnexus",  // 原 omnigent
}
```

### Phase 4: 环境变量

批量替换所有环境变量前缀：
```bash
# Python 文件
find . -name "*.py" -exec sed -i 's/OMNIGENT_/AGENTNEXUS_/g' {} +

# Shell 脚本
find . -name "*.sh" -exec sed -i 's/OMNIGENT_/AGENTNEXUS_/g' {} +

# YAML/TOML 配置
find . -name "*.yaml" -o -name "*.yml" -o -name "*.toml" -exec sed -i 's/OMNIGENT_/AGENTNEXUS_/g' {} +
```

常见变量映射：
- `OMNIGENT_CONFIG_HOME` → `AGENTNEXUS_CONFIG_HOME`
- `OMNIGENT_DATA_DIR` → `AGENTNEXUS_DATA_DIR`
- `OMNIGENT_AUTH_ENABLED` → `AGENTNEXUS_AUTH_ENABLED`
- `OMNIGENT_SERVER_URL` → `AGENTNEXUS_SERVER_URL`
- 等等...

### Phase 5: 配置路径

#### 5.1 默认配置目录
```python
# 从 ~/.omnigent/ 改为 ~/.agentnexus/
DEFAULT_CONFIG_DIR = Path.home() / ".agentnexus"
DEFAULT_DATA_DIR = Path.home() / ".agentnexus" / "data"
```

#### 5.2 项目级配置
```bash
# 从 .omnigent/ 改为 .agentnexus/
find . -name "*.py" -exec sed -i 's/\.omnigent/.agentnexus/g' {} +
```

### Phase 6: 数据库和持久化

#### 6.1 Alembic 迁移
创建新迁移脚本处理：
- 表名前缀（如果有）
- 配置表中的字符串引用
- 序列化数据中的包名

#### 6.2 数据库连接字符串
```python
# SQLAlchemy 连接
# 检查是否有 omnigent 数据库名
```

### Phase 7: URL 和域名引用

```bash
# GitHub 仓库 URL
find . -type f -exec sed -i 's|omnigent-ai/omnigent|K4y2020/AgentNexus|g' {} +
find . -type f -exec sed -i 's|github.com/omnigent-ai|github.com/K4y2020|g' {} +

# 文档 URL
find . -name "*.md" -exec sed -i 's|omnigent\.ai|agentnexus.dev|g' {} +  # 如果有新域名
```

### Phase 8: Docker 和部署

#### 8.1 Docker 镜像名
```dockerfile
# 从 omnigent:latest 改为 agentnexus:latest
```

#### 8.2 Kubernetes 资源
```yaml
# deploy/kubernetes/*.yaml
name: agentnexus
image: agentnexus/server:latest
```

### Phase 9: 前端代码

#### 9.1 包名和标题
```json
// web/package.json
{
  "name": "@agentnexus/web",
}
```

```html
<!-- web/index.html -->
<title>AgentNexus</title>
```

#### 9.2 TypeScript/JavaScript 引用
```bash
find web/ -name "*.ts" -o -name "*.tsx" -o -name "*.js" -exec sed -i 's/omnigent/agentnexus/g' {} +
```

### Phase 10: 文档和注释

```bash
# Markdown 文档
find . -name "*.md" -exec sed -i 's/Omnigent/AgentNexus/g' {} +
find . -name "*.md" -exec sed -i 's/omnigent/agentnexus/g' {} +

# Python docstrings 和注释
find . -name "*.py" -exec sed -i 's/Omnigent/AgentNexus/g' {} +
```

### Phase 11: 测试文件

```bash
# 测试目录
find tests/ -name "*.py" -exec sed -i 's/omnigent/agentnexus/g' {} +
```

### Phase 12: CI/CD

#### 12.1 GitHub Actions
```yaml
# .github/workflows/*.yml
- 更新工作流中的包名引用
- 更新 Docker 镜像名
- 更新环境变量
```

#### 12.2 Pre-commit hooks
```yaml
# .pre-commit-config.yaml
# 检查是否有 repo URL 更新
```

## 验证清单

### 构建验证
- [ ] `uv sync` 成功
- [ ] `python -c "import agentnexus"` 成功
- [ ] `agentnexus --version` 运行
- [ ] `nexus --version` 运行

### 测试验证
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] E2E 测试通过

### 功能验证
- [ ] CLI 命令正常工作
- [ ] Web UI 启动正常
- [ ] 数据库迁移成功
- [ ] 配置文件加载正常

### 部署验证
- [ ] Docker 构建成功
- [ ] 本地安装测试
- [ ] 升级路径测试（从旧版本）

## 迁移指南（给用户）

### 自动迁移脚本
```python
# agentnexus/migration/from_omnigent.py
def migrate_config():
    """将 ~/.omnigent/ 迁移到 ~/.agentnexus/"""
    old_dir = Path.home() / ".omnigent"
    new_dir = Path.home() / ".agentnexus"
    
    if old_dir.exists() and not new_dir.exists():
        print(f"Migrating {old_dir} → {new_dir}")
        shutil.copytree(old_dir, new_dir)
        # 更新配置文件中的路径引用
```

### 环境变量兼容层
```python
# 临时支持旧变量名（1-2 个版本）
def get_config_value(key):
    new_var = f"AGENTNEXUS_{key}"
    old_var = f"OMNIGENT_{key}"
    
    value = os.getenv(new_var) or os.getenv(old_var)
    if os.getenv(old_var):
        warnings.warn(
            f"{old_var} is deprecated, use {new_var}",
            DeprecationWarning
        )
    return value
```

## 风险缓解

1. **回滚计划**
   - 保持 git 历史完整
   - 可以 `git revert` 回滚

2. **渐进式发布**
   - 先发布 alpha 版本测试
   - 收集反馈后正式发布

3. **文档更新**
   - 更新所有安装文档
   - 提供迁移指南
   - 标注破坏性变更

## 预计时间线

- Phase 1-2: 1 小时（包重命名）
- Phase 3-6: 4 小时（配置和路径）
- Phase 7-10: 3 小时（URL、部署、前端）
- Phase 11-12: 2 小时（测试和 CI）
- 验证测试: 4 小时
- 文档更新: 2 小时

**总计**: 16-20 小时

## 开始执行？

准备好后，我将按顺序执行上述步骤。建议先创建专门的重命名分支：

```bash
git checkout -b feature/rename-to-agentnexus
git commit -am "checkpoint before rename"
```
