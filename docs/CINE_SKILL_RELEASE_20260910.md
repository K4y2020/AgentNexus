# Cine Skill 发布与真实 Topic 验证

评测归因更正见 [Cine 分层审计](CINE_LAYER_ATTRIBUTION_AUDIT.md)：发布与加载事实仍成立，“删邻居证明剧本编排缺陷”的推断撤回。

## 发布结果

共享 Cine 已从 v23 更新为 **v25**，agent ID 保持 `7236cdb3ad73464b947752d732dcd566`，Bot ID 保持 `50cbe1836fb7541da95e4f859b447ae5`。

重启前完整查询会话列表，无运行、等待或后台任务；沿用原数据库、artifact 路径及服务器配置启动 6767，通过原生 `--agent` 注册发布包。服务和宿主均已恢复在线，未修改默认模型，未删除会话。

下载原有 Cine 会话的实际 agent 包核对，四个运行文件与当前源文件逐字节一致：`config.yaml`、两个剧本/分镜 `SKILL.md`、`references/h3-prompt.md`。实际包另含已审核的分镜 `selftest.mjs` 更新，已核对与源文件一致；其余包文件不变。

原包、发布清单、发布后包及文件哈希保存在 `outputs/cine-publish-20260910/`。`previous-bundle.tar.gz` 是回退备份；没有备份或覆盖整个运行数据库。

## 发布后测试

两个 Topic 直接绑定共享 Cine v25，不上传临时 agent 包。测试单独指定 Luna，不改变日常模型偏好；模型事实记录 requested/resolved 均为 `gpt-5.6-luna`，上游身份仍未由网关公开。

- `72b06c96ee8ac96e5b5e2d9147e7bf7e`：自然剧本，无逐动作时间表；分镜为 6/6/8 秒，总计 20 秒；两句对白保留。调用了两次 Skill，未完全遵守测试的“不调用工具”要求。
- `4c70bd6b91fbae93a5d92ef95cb7d621`：识别道具来源与交接缺口，接受回忆中的状态变化；保留第 4 秒铃响、第 6 秒合书及单镜头要求。没有工具调用。

两次均为首次输出、已回到 idle，没有纠正后重跑，没有生图、视频或子 agent 派发。完整回答与运行记录保存在同一发布目录。

## 结论与剩余风险

**发布及加载验证完成；创作能力不作全量 PASS。** 新规则已进入真实 Bot Topic，而不只是源码或独立验收包。混合审查请求中的删除邻居建议不能作为纯剧本生成退化或反向模板注入的证据；单次文字测试也不证明成片质量或长期稳定性。

日常验证建议新建 Cine Topic，要求简洁剧本加分镜计划时长，检查是否保持自然叙事。已有会话的历史和旧稿不会被此次发布清除；避免把旧上下文造成的影响误认为新包未发布。
