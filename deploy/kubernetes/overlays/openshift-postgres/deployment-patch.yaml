apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentnexus
spec:
  template:
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: agentnexus
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
