apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: agentnexus-postgres
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: agentnexus-postgres
    component: postgres
  ports:
    - port: 5432
      targetPort: postgres
      protocol: TCP
      name: postgres
