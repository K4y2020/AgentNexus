apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentnexus-artifacts
  labels:
    app: agentnexus
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
