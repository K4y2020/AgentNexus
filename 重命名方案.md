# AgentNexus → AgentNexus 重命名方案

## 现状分析

- **643 个文件**包含 "agentnexus"
- **9097 处**引用
- 涉及：Python 包名、配置路径、环境变量、文档、URL、数据库表名等

## 重命名策略分级

### 🔴 高风险 - 暂不修改（会破坏兼容性）

1. **Python 包名** `agentnexus` → `agentnexus`
   - 影响：所有 `import agentnexus.*` 语句
   - 代价：需要重写 12,212 个 Python 文件
   - 建议：保持 `agentnexus` 作为内部包名

2. **配置目录** `~/.agentnexus/` → `~/.agentnexus/`
   - 影响：用户数据迁移
   - 代价：破坏现有安装
   - 建议：保持兼容性或提供自动迁移

3. **环境变量** `AGENTNEXUS_*` → `AGENTNEXUS_*`
   - 影响：所有部署配置
   - 代价：文档与脚本重写
   - 建议：双变量支持，逐步废弃旧变量

4. **数据库表名/字段**
   - 影响：需要数据库迁移
   - 代价：生产环境数据迁移风险
   - 建议：暂不修改

### 🟡 中风险 - 可选修改

1. **CLI 命令名**
   - 现状：`agentnexus` / `omni`
   - 建议：添加 `agentnexus` / `nexus` 作为别名（已完成）
   - 保留旧命令向后兼容

2. **PyPI 包名**
   - 现状：`agentnexus`
   - 建议：发布为 `agentnexus`，标记 `agentnexus` 为废弃

### 🟢 低风险 - 立即修改

1. **文档中的产品名称**
   - README.md
   - CONTRIBUTING.md
   - docs/*.md

2. **注释和文档字符串**
   - Python docstrings
   - 代码注释

3. **URL 和仓库引用**
   - GitHub URL
   - 文档链接

4. **用户可见的 UI 文本**
   - Web UI 标题
   - CLI 帮助文本
   - 错误消息

## 推荐执行方案

### 阶段 1: 品牌重命名（安全，立即执行）

修改所有**用户可见**的文本，但保持代码层面的 `agentnexus`：

```bash
# 文档中的产品名
README.md: "AgentNexus" → "AgentNexus"
CLAUDE.md: "AgentNexus" → "AgentNexus"
docs/*.md: 所有产品引用

# URL 更新
github.com/agentnexus-ai/agentnexus → github.com/K4y2020/AgentNexus

# Web UI
web/index.html: <title>
web/src/**/*.tsx: 界面文本

# CLI 帮助文本
cli.py: help strings
```

**优点**：
- 用户看到的是 AgentNexus
- 不破坏任何导入或配置
- 可以随时回滚

### 阶段 2: 添加别名（向后兼容）

```python
# pyproject.toml
[project.scripts]
agentnexus = "agentnexus.cli:main"  # 新增
nexus = "agentnexus.cli:main"        # 新增
agentnexus = "agentnexus.cli:main"    # 保留
omni = "agentnexus.cli:main"        # 保留
```

```python
# 环境变量双支持
AGENTNEXUS_* = os.getenv("AGENTNEXUS_*") or os.getenv("AGENTNEXUS_*")
```

### 阶段 3: 包名重命名（破坏性，需要主版本升级）

仅在 2.0 版本时执行：
1. `agentnexus` → `agentnexus` 包重命名
2. 数据库迁移工具
3. 配置自动迁移
4. 弃用警告

## 当前建议：仅执行阶段 1

保持代码稳定性，只改品牌相关的文本。

## 预计工作量

- 阶段 1: 2-4 小时（文档和 UI 文本）
- 阶段 2: 4-8 小时（别名和兼容层）
- 阶段 3: 40-80 小时（完整包重命名 + 测试）

## 风险评估

| 方案 | 用户影响 | 技术债务 | 推荐度 |
|------|---------|---------|--------|
| 仅改文档 | ✅ 无影响 | ⚠️ 包名不一致 | ⭐⭐⭐⭐⭐ |
| 添加别名 | ✅ 向后兼容 | ⚠️ 维护双路径 | ⭐⭐⭐⭐ |
| 完整重命名 | ❌ 破坏兼容 | ✅ 彻底清理 | ⭐⭐ (2.0 版本) |
