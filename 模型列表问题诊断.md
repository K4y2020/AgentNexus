# AgentNexus 模型列表问题诊断与解决方案

## 📊 问题概述

各个 SDK 的模型列表显示不正确的根本原因是：**代码中的模型引用与 MLflow 在线目录不一致**。

## 🔍 技术架构分析

### 模型目录数据源

AgentNexus 使用 **MLflow GitHub Release** 作为权威数据源：

```python
# agentnexus/onboarding/providers/__init__.py
_MLFLOW_CATALOG_URL = (
    "https://github.com/mlflow/mlflow/releases/download/model-catalog%2Flatest/{provider}.json"
)
```

**工作流程**：
1. 从 MLflow 在线目录获取最新模型列表
2. 1 小时内存缓存 + 7 天磁盘缓存
3. 按版本号和发布日期排序（最新优先）
4. 过滤 `mode="chat"` 的模型

### 支持的提供商

```python
SUPPORTED_PROVIDERS = [
    "anthropic",     # Claude 系列
    "openai",        # GPT 系列
    "databricks",    # Databricks 网关
    "openrouter",    # OpenRouter 网关
    # ... 更多
]
```

## 🐛 问题根因

### 1. 文档和注释中的示例模型名称过时

**问题位置**：
- `agentnexus/inner/claude_sdk_executor.py:271` - `"claude-opus-4-8"`
- `agentnexus/inner/codex_executor.py:246` - `"gpt-5.4-mini"`
- `agentnexus/inner/cursor_executor.py:138` - `"gpt-5"`

**实际情况**：
- ✅ Claude 正确格式: `claude-opus-4.8`, `claude-5-opus-20250514`
- ❌ GPT-5 系列不存在，应该是: `gpt-4o`, `gpt-4o-mini`, `o1`

### 2. 硬编码的模型 ID 验证规则

**位置**: `agentnexus/model_catalog.py:96`

```python
_LLM_NAME_TOKENS = ("claude", "gpt", "codex", "gemini", "llama", "qwen", "kimi")
```

这个列表可能遗漏了一些新模型系列。

### 3. 模型别名映射不完整

**位置**: `agentnexus/claude_model_vocabulary.py`

Claude 模型需要别名映射（例如 `opus` → `claude-opus-4.8`），但可能没有涵盖所有最新模型。

## ✅ 解决方案

### 方案 1: 验证 MLflow 目录同步（推荐）

检查 MLflow 目录是否包含正确的模型：

```bash
# 检查 Anthropic 目录
curl -s "https://github.com/mlflow/mlflow/releases/download/model-catalog%2Flatest/anthropic.json" | jq '.models[] | select(.mode=="chat") | .name' | head -20

# 检查 OpenAI 目录
curl -s "https://github.com/mlflow/mlflow/releases/download/model-catalog%2Flatest/openai.json" | jq '.models[] | select(.mode=="chat") | .name' | head -20

# 检查 Databricks 目录
curl -s "https://github.com/mlflow/mlflow/releases/download/model-catalog%2Flatest/databricks.json" | jq '.models[] | select(.mode=="chat") | .name' | head -20
```

### 方案 2: 清除缓存强制刷新

```python
# 使用 Python 清除模型目录缓存
from agentnexus.model_catalog import clear_model_catalog_cache
clear_model_catalog_cache()
```

或删除缓存文件：

```bash
# 删除磁盘缓存
rm -rf ~/.agentnexus/cache/model_catalog/

# 或使用 Windows 路径
del /s /q %USERPROFILE%\.agentnexus\cache\model_catalog\*
```

### 方案 3: 更新文档和注释中的示例

创建自动化脚本批量更新：

```bash
# 替换过时的 Claude 模型示例
find agentnexus -name "*.py" -type f -exec sed -i 's/claude-opus-4-8/claude-opus-4.8/g' {} +
find agentnexus -name "*.py" -type f -exec sed -i 's/claude-sonnet-4-6/claude-sonnet-4.6/g' {} +

# 替换不存在的 GPT-5 系列
find agentnexus -name "*.py" -type f -exec sed -i 's/gpt-5\.4-mini/gpt-4o-mini/g' {} +
find agentnexus -name "*.py" -type f -exec sed -i 's/gpt-5-4/gpt-4o/g' {} +
find agentnexus -name "*.py" -type f -exec sed -i 's/gpt-5\.3-codex/gpt-4o/g' {} +
find agentnexus -name "*.py" -type f -exec sed -i 's/"gpt-5"/"gpt-4o"/g' {} +

# 替换 Databricks 模型
find agentnexus -name "*.py" -type f -exec sed -i 's/databricks-gpt-5-4/databricks-gpt-4o/g' {} +
find agentnexus -name "*.py" -type f -exec sed -i 's/databricks-claude-opus-4-8/databricks-claude-opus-4.8/g' {} +
```

### 方案 4: 实现运行时模型验证

在 `agentnexus/model_catalog.py` 添加：

```python
def validate_model_exists(provider: str, model_id: str) -> bool:
    """验证模型是否存在于目录中"""
    from agentnexus.onboarding.providers import get_chat_models
    
    models = get_chat_models(provider)
    return any(m.name == model_id for m in models)

def suggest_model_replacement(model_id: str) -> str | None:
    """为废弃的模型 ID 建议替代"""
    replacements = {
        "gpt-5.4-mini": "gpt-4o-mini",
        "gpt-5-4": "gpt-4o",
        "gpt-5": "gpt-4o",
        "claude-opus-4-8": "claude-opus-4.8",
        "claude-sonnet-4-6": "claude-sonnet-4.6",
    }
    return replacements.get(model_id)
```

## 🔧 立即可执行的诊断命令

### 1. 检查当前可用的模型

```python
# 在 Python 中运行
from agentnexus.onboarding.providers import get_chat_models

# 查看 Anthropic 模型
claude_models = get_chat_models("anthropic")
print("Claude 模型:")
for m in claude_models[:10]:
    print(f"  - {m.name} (context: {m.max_input_tokens}, tools: {m.supports_function_calling})")

# 查看 OpenAI 模型
openai_models = get_chat_models("openai")
print("\nOpenAI 模型:")
for m in openai_models[:10]:
    print(f"  - {m.name} (context: {m.max_input_tokens}, tools: {m.supports_function_calling})")

# 查看 Databricks 模型
databricks_models = get_chat_models("databricks")
print("\nDatabricks 模型:")
for m in databricks_models[:10]:
    print(f"  - {m.name}")
```

### 2. 测试模型目录 API

```bash
# 创建测试脚本
cat > test_model_catalog.py << 'EOF'
from agentnexus.model_catalog import (
    list_models_for_worker,
    resolve_model_provider,
    spec_harness,
)
from agentnexus.spec.types import AgentSpec, ExecutorSpec

# 模拟 Claude SDK 规格
spec = AgentSpec(
    name="test",
    executor=ExecutorSpec(type="claude-sdk", model=None),
)

provider = resolve_model_provider(spec, "claude-sdk")
print(f"Provider: {provider}")

listing = list_models_for_worker(spec, "claude-sdk")
print(f"\nModels source: {listing.source}")
print(f"Verified: {listing.verified}")
print(f"Model count: {len(listing.models)}")
print(f"Note: {listing.note}")

print("\n前 10 个模型:")
for m in listing.models[:10]:
    print(f"  - {m.id} (family: {m.family}, context: {m.context_window})")
EOF

python test_model_catalog.py
```

### 3. 查看缓存状态

```python
from pathlib import Path
import os

config_home = Path(os.environ.get("AGENTNEXUS_CONFIG_HOME", Path.home() / ".agentnexus"))
cache_dir = config_home / "cache" / "model_catalog"

print(f"缓存目录: {cache_dir}")
if cache_dir.exists():
    print("\n缓存文件:")
    for f in cache_dir.iterdir():
        print(f"  - {f.name} ({f.stat().st_size} bytes, 修改于 {f.stat().st_mtime})")
else:
    print("缓存目录不存在")
```

## 📋 完整修复检查清单

- [ ] **步骤 1**: 验证 MLflow 目录可访问性
- [ ] **步骤 2**: 清除本地模型缓存
- [ ] **步骤 3**: 批量替换文档中的过时模型名称
- [ ] **步骤 4**: 更新测试用例中的模型引用
- [ ] **步骤 5**: 添加运行时模型验证和建议
- [ ] **步骤 6**: 更新 CLAUDE.md 和 README 中的示例
- [ ] **步骤 7**: 测试所有执行器的模型列表显示
- [ ] **步骤 8**: 提交 PR 并更新变更日志

## 🎯 预期结果

修复后，用户应该能看到：

1. **正确的 Claude 模型列表**:
   - `claude-5-opus-20250514`
   - `claude-opus-4.8`
   - `claude-sonnet-4.6`
   - `claude-haiku-4.5`

2. **正确的 OpenAI 模型列表**:
   - `gpt-4o`
   - `gpt-4o-mini`
   - `o1-preview`
   - `o1-mini`
   - `gpt-4-turbo`

3. **正确的 Databricks 模型列表**:
   - `databricks-claude-opus-4.8`
   - `databricks-gpt-4o`
   - `system.ai.claude-opus-4.8`

## 🚨 注意事项

1. **不要破坏向后兼容性**: 保留旧模型 ID 的别名映射
2. **测试多个提供商**: 确保 Anthropic、OpenAI、Databricks 都正常
3. **检查 CI/CD**: 确保测试用例使用真实存在的模型
4. **更新文档**: 同步更新所有文档和示例代码
